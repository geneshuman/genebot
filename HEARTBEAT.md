# HEARTBEAT.md

- **Email Monitoring**: Check `genebot@entropyandsons.com` for new messages from `gene@entropyandsons.com`. If new replies exist, notify Gene here and on Discord. Track `lastChecks.email` in `memory/heartbeat-state.json`.
